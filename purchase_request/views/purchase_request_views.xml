<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================== -->
    <!--  Purchase Request Form View    -->
    <!-- ============================== -->
    <record id="view_purchase_request_form" model="ir.ui.view">
        <field name="name">purchase.request.form</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <form string="Purchase Request">
                <header>
                    <button name="action_submit"
                            type="object"
                            string="Submit Request"
                            class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_approve"
                            type="object"
                            string="Approve"
                            class="oe_highlight"
                            invisible="state != 'submitted'"
                            groups="purchase.group_purchase_manager"/>
                    <button name="action_create_rfq"
                            type="object"
                            string="Create RFQ"
                            class="oe_highlight"
                            invisible="state != 'approved'"
                            groups="purchase.group_purchase_user"/>
                    <button name="action_reject"
                            type="object"
                            string="Reject"
                            invisible="state != 'submitted'"
                            groups="purchase.group_purchase_manager"/>
                    <button name="action_cancel"
                            type="object"
                            string="Cancel"
                            invisible="state not in ('draft', 'submitted', 'approved')"/>
                    <button name="action_reset_draft"
                            type="object"
                            string="Reset to Draft"
                            invisible="state not in ('rejected', 'cancelled')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,approved,rfq_created"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_rfq"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-file-text-o"
                                invisible="rfq_count == 0">
                            <field name="rfq_count" widget="statinfo" string="RFQ"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="employee_id"
                                   readonly="state != 'draft'"
                                   options="{'no_create': True}"/>
                            <field name="department_id" readonly="1"/>
                            <field name="manager_id" readonly="1"/>
                            <field name="priority"
                                   readonly="state not in ('draft', 'submitted')"
                                   widget="priority"/>
                        </group>
                        <group>
                            <field name="request_date" readonly="state != 'draft'"/>
                            <field name="date_required" readonly="state not in ('draft', 'submitted')"/>
                            <field name="company_id" invisible="1"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="estimated_total" widget="monetary"/>
                            <field name="approved_by" readonly="1"
                                   invisible="not approved_by"/>
                            <field name="approved_date" readonly="1"
                                   invisible="not approved_date"/>
                        </group>
                    </group>

                    <group>
                        <field name="description"
                               placeholder="Explain the purpose and justification for this purchase request..."
                               readonly="state not in ('draft', 'submitted')"/>
                    </group>

                    <notebook>
                        <page string="Request Lines" name="request_lines">
                            <field name="line_ids"
                                   readonly="state not in ('draft',)">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="product_id"
                                           domain="[('purchase_ok', '=', True)]"
                                           required="1"/>
                                    <field name="description"/>
                                    <field name="quantity" required="1"/>
                                    <field name="product_uom_id"
                                           optional="show"/>
                                    <field name="product_uom_category_id" column_invisible="1"/>
                                    <field name="estimated_unit_price"/>
                                    <field name="currency_id" column_invisible="1"/>
                                    <field name="estimated_cost" widget="monetary"/>
                                    <field name="specifications" optional="hide"/>
                                </list>
                            </field>

                            <group class="oe_subtotal_footer">
                                <field name="estimated_total" widget="monetary"
                                       class="oe_subtotal_footer_separator"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ============================== -->
    <!--  Purchase Request List View    -->
    <!-- ============================== -->
    <record id="view_purchase_request_list" model="ir.ui.view">
        <field name="name">purchase.request.list</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <list string="Purchase Requests"
                  decoration-info="state in ('draft', 'submitted')"
                  decoration-success="state in ('approved', 'rfq_created')"
                  decoration-danger="state == 'rejected'"
                  decoration-muted="state == 'cancelled'"
                  sample="1">
                <field name="priority" widget="priority" nolabel="1" optional="show"/>
                <field name="name" decoration-bf="1"/>
                <field name="employee_id"/>
                <field name="department_id"/>
                <field name="request_date"/>
                <field name="date_required" optional="show"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="estimated_total" widget="monetary" optional="show"/>
                <field name="rfq_id" optional="show"/>
                <field name="state"
                       decoration-info="state in ('draft', 'submitted')"
                       decoration-success="state in ('approved', 'rfq_created')"
                       decoration-danger="state == 'rejected'"
                       widget="badge"/>
            </list>
        </field>
    </record>

    <!-- ============================== -->
    <!--  Purchase Request Search View  -->
    <!-- ============================== -->
    <record id="view_purchase_request_search" model="ir.ui.view">
        <field name="name">purchase.request.search</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <search string="Search Purchase Requests">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="department_id"/>
                <separator/>
                <filter name="my_requests" string="My Requests"
                        domain="[('employee_id.user_id', '=', uid)]"/>
                <filter name="my_department" string="My Department"
                        domain="[('department_id.member_ids.user_id', '=', uid)]"/>
                <separator/>
                <filter name="draft" string="Draft" domain="[('state', '=', 'draft')]"/>
                <filter name="submitted" string="Submitted"
                        domain="[('state', '=', 'submitted')]"/>
                <filter name="approved" string="Approved"
                        domain="[('state', '=', 'approved')]"/>
                <filter name="rfq_created" string="RFQ Created"
                        domain="[('state', '=', 'rfq_created')]"/>
                <filter name="urgent" string="Urgent"
                        domain="[('priority', 'in', ('1', '2'))]"/>
                <separator/>
                <filter name="pending_approval" string="Pending My Approval"
                        domain="[('state', '=', 'submitted'), ('manager_id.user_id', '=', uid)]"/>
                <separator/>
                <filter invisible="1" string="Late Activities" name="activities_overdue"
                        domain="[('my_activity_date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"/>
                <filter invisible="1" string="Today Activities" name="activities_today"
                        domain="[('my_activity_date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter invisible="1" string="Future Activities" name="activities_upcoming_all"
                        domain="[('my_activity_date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))]"/>
                <separator/>
                <group expand="0" string="Group By">
                    <filter string="Employee" name="group_employee"
                            context="{'group_by': 'employee_id'}"/>
                    <filter string="Department" name="group_department"
                            context="{'group_by': 'department_id'}"/>
                    <filter string="Status" name="group_state"
                            context="{'group_by': 'state'}"/>
                    <filter string="Priority" name="group_priority"
                            context="{'group_by': 'priority'}"/>
                    <filter string="Request Date" name="group_date"
                            context="{'group_by': 'request_date'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- ============================== -->
    <!--  Purchase Request Kanban View  -->
    <!-- ============================== -->
    <record id="view_purchase_request_kanban" model="ir.ui.view">
        <field name="name">purchase.request.kanban</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" sample="1" quick_create="false">
                <field name="currency_id"/>
                <progressbar field="activity_state"
                             colors='{"planned": "success", "today": "warning", "overdue": "danger"}'/>
                <templates>
                    <t t-name="card">
                        <div class="d-flex align-items-baseline">
                            <field name="priority" widget="priority" class="me-1"/>
                            <field name="name" class="fw-bolder me-2"/>
                            <field name="estimated_total" widget="monetary"
                                   class="fw-bolder ms-auto"/>
                        </div>
                        <div class="text-muted">
                            <field name="employee_id"/> -
                            <field name="department_id"/>
                        </div>
                        <footer>
                            <field name="request_date"/>
                            <field name="activity_ids" widget="kanban_activity"/>
                            <field name="state" widget="label_selection"
                                   options="{'classes': {'draft': 'default', 'submitted': 'info', 'approved': 'success', 'rfq_created': 'success', 'rejected': 'danger', 'cancelled': 'default'}}"
                                   class="ms-auto"/>
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- ============================== -->
    <!--  Purchase Request Actions      -->
    <!-- ============================== -->

    <!-- All Requests (for procurement) -->
    <record id="action_purchase_request_all" model="ir.actions.act_window">
        <field name="name">Purchase Requests</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_purchase_request_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No purchase requests yet.
            </p>
            <p>
                Purchase requests are submitted by employees who need to procure goods or services.
                The procurement team reviews and creates RFQs from approved requests.
            </p>
        </field>
    </record>

    <!-- My Requests (for employees) -->
    <record id="action_purchase_request_my" model="ir.actions.act_window">
        <field name="name">My Purchase Requests</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_purchase_request_search"/>
        <field name="context">{'search_default_my_requests': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first purchase request!
            </p>
            <p>
                Submit a request when you need to purchase products or services.
                Your request will be reviewed by the procurement department.
            </p>
        </field>
    </record>

    <!-- Pending Approval (for managers) -->
    <record id="action_purchase_request_to_approve" model="ir.actions.act_window">
        <field name="name">Requests to Approve</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_purchase_request_search"/>
        <field name="domain">[('state', '=', 'submitted')]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No pending purchase requests.
            </p>
            <p>All purchase requests have been processed.</p>
        </field>
    </record>

    <!-- Approved - Ready for RFQ (for procurement) -->
    <record id="action_purchase_request_approved" model="ir.actions.act_window">
        <field name="name">Approved Requests (Ready for RFQ)</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_purchase_request_search"/>
        <field name="domain">[('state', '=', 'approved')]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No approved requests pending RFQ creation.
            </p>
        </field>
    </record>

    <!-- ============================== -->
    <!--  Menu Items                    -->
    <!-- ============================== -->

    <!-- Top-level menu under Purchase -->
    <menuitem id="menu_purchase_request_root"
              name="Purchase Requests"
              parent="purchase.menu_purchase_root"
              sequence="3"/>

    <menuitem id="menu_purchase_request_all"
              name="All Requests"
              parent="menu_purchase_request_root"
              action="action_purchase_request_all"
              sequence="1"
              groups="purchase.group_purchase_user"/>

    <menuitem id="menu_purchase_request_my"
              name="My Requests"
              parent="menu_purchase_request_root"
              action="action_purchase_request_my"
              sequence="2"/>

    <menuitem id="menu_purchase_request_to_approve"
              name="To Approve"
              parent="menu_purchase_request_root"
              action="action_purchase_request_to_approve"
              sequence="3"
              groups="purchase.group_purchase_manager"/>

    <menuitem id="menu_purchase_request_approved"
              name="Ready for RFQ"
              parent="menu_purchase_request_root"
              action="action_purchase_request_approved"
              sequence="4"
              groups="purchase.group_purchase_user"/>

</odoo>
